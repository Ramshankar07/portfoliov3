---
target: index.html
total_score: 23
max_score: 40
na_heuristics: 
p0_count: 2
p1_count: 2
target_identity: "file:/Users/ram/Public/portfolio/portfoliov3/index.html"
target_fingerprint: "sha256:3c7fcf1b0def34a37c48c2af9b236eb9af6a269819cdcefc1afb06f00d758310"
target_path: /Users/ram/Public/portfolio/portfoliov3/index.html
timestamp: 2026-09-19T01-19-29Z
slug: index-html
closed: true
---
Method: dual-agent (A: design review · B: detector + browser evidence)

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|---|---|---|
| 1 | Visibility of System Status | 2 | Reveal-gated content sits at opacity:0 with no loading signal |
| 2 | Match System / Real World | 3 | Reader's own language; nav labels don't match section titles |
| 3 | User Control and Freedom | 1 | Agent Mode: Escape doesn't close, focus never enters, ✕ unreachable by tabbing |
| 4 | Consistency and Standards | 2 | Contact links compute text-decoration:none in a no-color system — zero link affordance |
| 5 | Error Prevention | 2 | Phone isn't tel:; nothing prevents mobile arriving scrolled past the pitch |
| 6 | Recognition Rather Than Recall | 3 | Baselines inline everywhere; two unlabeled icon buttons |
| 7 | Flexibility and Efficiency | 2 | Applicable. Filter barely filters; search is least trustworthy control |
| 8 | Aesthetic and Minimalist Design | 3 | Disciplined monochrome; three competing above-fold affordances |
| 9 | Error Recovery | 2 | Only error surface is the chat, posting to a staging Worker |
| 10 | Help and Documentation | 3 | Applicable. Excellent docs, aimed entirely at the machine audience |
| **Total** | | **23/40** | **Acceptable — significant improvements needed** |

Both 7 and 10 judged applicable; denominator is the full 40. Technical audit scored 19/20 the same day — the implementation is nearly flawless, the design decisions are not.

## Design Specificity Verdict

Specific in the evidence layer, category-default in the conversion layer.

Authored and not liftable: the metric callout (caption slot structurally prevents a bare multiplier — the "every number names its baseline" principle compiled into component geometry), the hand-authored SVG diagram at currentColor, the featured-card metric rail, the 01.–05. broadsheet furniture, the Agent Mode console.

Liftable unchanged: the entire hero ("Hi, I'm [Name]" + lead + two CTAs + circular portrait + animated background = the 2019–2024 developer-portfolio template), the filter chips, the icon-circle contact rows, the dotted timeline.

Deterministic scan: CLI 15 findings (exit 2). 7 confirmed, 8 false positives.
- Confirmed: all-caps-body; gpt-thin-border-wide-shadow ×2 (:245 portrait, :977 agent panel); image-hover-transform (:245); pulsing-dot ×1 (:988); design-system-font (01.–05. mono family used by 53 elements, never declared in DESIGN.md).
- False positives: overused-font inter (91% measured but a recorded binding brand commitment); pulsing-dot .typing-indicator (orphaned CSS, zero occurrences in any HTML/JS); nested-cards ×3 (not reproducible in live DOM, two probes); design-system-font-size ×4 (styles.css:961-964 forces all to 12px).

Visual overlays: injection succeeded fully, no CSP. 44 overlay nodes rendered. Console: 45 anti-patterns. Browser run surfaced 41 line-length findings the static scan missed (85–184 chars/line) and independently dropped nested-cards, design-system-font, design-system-font-size.

## Overall Impression

The evidence layer is excellent and the conversion layer is broken. A hiring manager reaches the project cards around second 25 and thinks "this person measures things" — the design earns that. At second 50 they click Experience and get two cards over a column of hollow dots on blank paper. The page is a beautifully typeset broadsheet with a stock cover and a stock back page.

## What's Working

- The metric callout is a product principle made structural. Compliance is the shape of the component.
- The monochrome system measures clean: 325 text nodes per theme, zero genuine AA failures (5.07:1 light, 7.13:1 dark lowest). Zero horizontal overflow at 320–1920. Zero console errors. One h1, no skipped heading levels across 38 headings, all 58 interactive elements have accessible names.
- The argument survives tag-stripping — complete case, in order, every number with its baseline. The one place the contract breaks (Experience) breaks for the human path, not the machine one.

## Priority Issues

[P0] Mobile auto-focus scrolls the headline, pitch, and both CTAs off screen before the visitor acts.
script.js:349 and :429 call chatInput.focus() on load. At 390×844 the page settles at scrollY=747; h1 at y=−667, lead at −525, both CTAs off screen. Keyboard opens over it on a real phone. Most first-touch traffic is mobile; those readers never see one word of positioning. Fix: drop load-time focus or gate behind matchMedia('(min-width: 768px)') with {preventScroll:true}. → /impeccable harden

[P0] Five of seven jobs render as empty space after a nav jump; all seven on a deep link.
Click Experience, wait 6s: opacities 1, 0.89, 0, 0, 0, 0, 0. Third card at y=779 — inside viewport — never resolves. Deep link /#experience: all seven at 0 after 3s. Under prefers-reduced-motion:reduce all seven render at 1. The nav jump and shared deep link are exactly how a triaging manager and a recruiter reach a work history. Fix: invert the reveal — resting opacity:1, observer adds the animation class. → /impeccable animate

[P1] At the conversion point, the only element styled as an action is the one that doesn't count as success.
Both #contact anchors compute text-decoration:none; with no color that leaves zero link affordance, visually identical to "Boston, MA" which isn't a link. The one underlined, uppercase, arrow-bearing element is BOOK ON CAL.COM — the secondary path. Phone isn't tel:. Fix: email as primary button with prefilled mailto: subject, LinkedIn secondary, phone tel:, Cal.com demoted. Right ~45% of the section is empty at 1440px. → /impeccable layout

[P1] Availability is invisible where it matters and contradicted where it isn't.
Hero says nothing about looking for work. Experience top card says "Aug 2026 – Present" — honest read is employed, started last month. Correction sits at screen 19.7 of 21 on mobile. DESIGN.md already specifies a status-pill and reserves the accent for "the live status dot"; the component exists and is not deployed. Fix: one hero pill — "Open to ML systems roles · Boston / remote". → /impeccable clarify

[P2] The h1 spends the page's largest type on zero information, for both audiences.
"Hi, I'm Ramshankar" is the sole H1 and the one permitted Display use. For the machine audience the site is instrumented for, H1 is the highest-weight string an ATS or LLM screener extracts. Fix: <h1>ML systems engineer — training and inference, down to the kernel</h1>, greeting demoted to serif-italic eyebrow. → /impeccable typeset

## Persona Red Flags

ML hiring manager (90 seconds, five tabs): 5 seconds on a greeting and a photo. Project cards at ~second 25 — where the site earns its money. Second 50: clicks Experience, gets hollow dots on blank paper. Tab closes there; the inbound email never happens.

Casey (distracted mobile): opens from LinkedIn, auto-scroll to y=747, keyboard pops. First screen is a photo and an empty input, zero words. Contact at y=16,668 — screen 19.7 of 21. No sticky contact affordance on mobile; left rail only above 1280px. Phone not tappable.

Sam (accessibility-dependent): skip link, scroll-spy contract, aria-pressed, 54/56 controls clear 44px, AA passes both themes. Then Agent Mode has no aria-expanded, role:null, aria-modal:null. Focus never enters the overlay; eight controls behind it stay focusable and invisible. Escape does nothing. Under prefers-reduced-motion the full work history renders — the accessible path is the only one that reliably shows it.

Jordan (first-timer): largest words are "Hi, I'm Ramshankar". Auto-focused search field wins the competition for first interaction, backed by a staging Cloudflare Worker.

## Minor Observations

- 🤖 📋 📄 render in full color inside H3s plus a robot in the footer — the only chromatic pixels on a site whose single binding commitment is "no chromatic accent, ever". They land in the accessible name.
- Flat-Always Rule violated in the render, not just markup: 28 shadow utilities; 12 elements carry resting shadows (9 × .timeline-card at 0 1px 2px, 2 × 0 25px 50px -12px, #mobile-nav-menu at 0 20px 25px -5px). On hover styles.css:484 re-asserts depth rather than removing it. Both halves of the rule break.
- Verification links (GITHUB / REPOSITORY / TECHNICAL BLOG) are 16px tall — the weakest-styled interactive elements, and the only way a skeptical reader checks a claim.
- Hero search input draws a raw 1px border inside the styled pill — visible box-in-a-box, both themes.
- Nav labels don't match section titles. "05. Get In Touch" immediately followed by "Let's Connect".
- 41 lines run 85–184 characters, above the 65–75ch measure DESIGN.md specifies.
- ASCII chip animation reads as stray glyph fragments near the right margin, especially light theme.
- Five chips over six cards: yields are 6/5/4/2/2. One card is tagged every bucket. "Training Systems" returns the Blackwell GEMM kernel card. On mobile the chips wrap to four rows (~220px) before a project is visible.
- Corrections to earlier session claims: the overlay CAN be dismissed by backdrop click (script.js:481-483, verified). Nav jump at 768px measures 69.0 → 82.6px, window 768–799px. hover:shadow-xl appears 16 times, not 17. #close-agent-mode is 20×24.5, #copy-prompt-btn is 95.7×16.

## Questions to Consider

1. What would this page look like designed backwards from a pre-filled mailto: — subject already naming the role and one of the numbers — with everything else as the case for clicking it?
2. Agent Mode hands an LLM "no large-cluster (100+ GPU) production experience." That's the most confident sentence on the site. Why does the human page have no equivalent moment?
3. The reduced-motion path is the only path that reliably renders the entire work history. If the most accessible version is also the most correct version, why is it the exception rather than the default?
4. PRODUCT principle 3 says the benchmarking apparatus is the work. Where is the timing table, the reward-hacking test matrix?
5. Experience says "Aug 2026 – Present." Contact says "I'm looking for work." They are 8,000 pixels apart. Which one does a hiring manager believe?
